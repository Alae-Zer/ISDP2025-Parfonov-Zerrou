<UserControl x:Class="ISDP2025_Parfonov_Zerrou.Forms.ForemanUserControls.ForemanInventoryControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ISDP2025_Parfonov_Zerrou.Forms.ForemanUserControls"
             mc:Ignorable="d" 
             d:DesignHeight="900" d:DesignWidth="800">

    <UserControl.Resources>
        <Style x:Key="RoundedButton" TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="5" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="White" Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left Side -->
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Foreman Inventory View" 
                   FontSize="24" 
                   FontWeight="Bold" 
                   Margin="0,0,0,20"
                   Grid.Row="0"/>

            <!-- Image Section -->
            <StackPanel Grid.Row="1" Margin="0,0,10,20">
                <Image x:Name="imgProduct" Height="150" Width="150" 
                       Margin="5" Stretch="Uniform"/>
                <StackPanel Orientation="Horizontal" Margin="5">
                    <TextBox x:Name="txtImageLocation" Width="200" IsReadOnly="True" Margin="0,0,10,0"/>
                    <Button x:Name="btnBrowseImage" Content="Browse" Width="60" 
                            Click="BtnBrowseImage_Click"/>
                </StackPanel>
            </StackPanel>

            <!-- Item Information -->
            <GroupBox Grid.Row="2" Header="Item Information" Padding="5" Margin="0,0,10,5">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Label Content="Item ID:" Grid.Row="0" Grid.Column="0"/>
                    <Label x:Name="lblItemId" Grid.Row="0" Grid.Column="1" Margin="5"/>

                    <Label Content="Item Name:" Grid.Row="1" Grid.Column="0"/>
                    <Label x:Name="lblItemName" Grid.Row="1" Grid.Column="1" Margin="5"/>

                    <Label Content="SKU:" Grid.Row="2" Grid.Column="0"/>
                    <Label x:Name="lblSku" Grid.Row="2" Grid.Column="1" Margin="5"/>

                    <Label Content="Category:" Grid.Row="3" Grid.Column="0"/>
                    <Label x:Name="lblCategory" Grid.Row="3" Grid.Column="1" Margin="5"/>

                    <Label Content="Supplier:" Grid.Row="4" Grid.Column="0"/>
                    <Label x:Name="lblSupplier" Grid.Row="4" Grid.Column="1" Margin="5"/>

                    <Label Content="Case Size:" Grid.Row="5" Grid.Column="0"/>
                    <Label x:Name="lblCaseSize" Grid.Row="5" Grid.Column="1" Margin="5"/>

                    <Label Content="Cost Price:" Grid.Row="6" Grid.Column="0"/>
                    <Label x:Name="lblCostPrice" Grid.Row="6" Grid.Column="1" Margin="5"/>

                    <Label Content="Retail Price:" Grid.Row="7" Grid.Column="0"/>
                    <Label x:Name="lblRetailPrice" Grid.Row="7" Grid.Column="1" Margin="5"/>

                    <Label Content="Description:" Grid.Row="8" Grid.Column="0"/>
                    <TextBox x:Name="txtDescription" Grid.Row="8" Grid.Column="1" 
                            Margin="5" TextWrapping="Wrap" AcceptsReturn="True" Height="60"/>

                    <Label Content="Notes:" Grid.Row="9" Grid.Column="0"/>
                    <TextBox x:Name="txtNotes" Grid.Row="9" Grid.Column="1" 
                            Margin="5" TextWrapping="Wrap" AcceptsReturn="True" Height="60"/>
                </Grid>
            </GroupBox>

            <!-- Buttons -->
            <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10">
                <Button x:Name="btnRefresh" Content="Refresh" Width="100" Height="30" 
                        Margin="0,0,10,0"
                        Click="BtnRefresh_Click"
                        Style="{StaticResource RoundedButton}"
                        Background="#2196F3"
                        Foreground="White" Cursor="Hand"/>
                <Button x:Name="btnClear" Content="Clear" Width="100" Height="30" 
                        Margin="0,0,10,0" Click="BtnClear_Click"
                        Style="{StaticResource RoundedButton}"
                        Background="#FFF62D2D"
                        Foreground="White" Cursor="Hand"/>
                <Button x:Name="btnUpdate" Content="Update" Width="100" Height="30" 
                        Margin="0,0,10,0" Click="BtnUpdate_Click"
                        Style="{StaticResource RoundedButton}"
                        Background="#FFD700"
                        Foreground="Black" Cursor="Hand"/>
                <Button x:Name="btnSave" Content="Save/Cancel" Width="100" Height="30" 
                        Click="BtnSave_Click"
                        Style="{StaticResource RoundedButton}"
                        Background="#4CAF50"
                        Foreground="White" Cursor="Hand"
                        Visibility="Collapsed"/>
            </StackPanel>
        </Grid>

        <!-- Right Side -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Search Panel -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                <Label Content="Search:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <TextBox x:Name="txtSearch" Width="200" Margin="0,0,10,0" 
                     TextChanged="TxtSearch_TextChanged" Cursor="Pen"/>
                <Label Content="Category:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <ComboBox x:Name="cmbSearchCategory" Width="150" Margin="0,0,10,0" 
                      SelectionChanged="CmbSearchCategory_SelectionChanged" Cursor="Hand"/>
            </StackPanel>

            <!-- DataGrid -->
            <DataGrid x:Name="dgvItems" 
                  Grid.Row="1" 
                  AutoGenerateColumns="False" 
                  IsReadOnly="True"
                  SelectionMode="Single"
                  SelectionChanged="DgInventory_SelectionChanged"
                  Margin="0,5,0,0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding ItemId}" Width="50">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="FontWeight" Value="Bold"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Item Name" Binding="{Binding ItemName}" Width="*"/>
                    <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="100"/>
                    <DataGridTextColumn Header="Supplier" Binding="{Binding SupplierName}" Width="120"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</UserControl>