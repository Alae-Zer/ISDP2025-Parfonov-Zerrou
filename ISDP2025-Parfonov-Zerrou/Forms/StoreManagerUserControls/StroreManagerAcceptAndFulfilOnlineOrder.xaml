<UserControl x:Class="ISDP2025_Parfonov_Zerrou.Forms.StoreManagerUserControls.StroreManagerAcceptAndFulfilOnlineOrder"
           xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
           xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
           xmlns:hc="https://handyorg.github.io/handycontrol"
           xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
           xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
           mc:Ignorable="d">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header Section -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Online Order Management" 
                   Style="{StaticResource TextBlockLarge}" 
                   Margin="5"/>

            <ComboBox Grid.Column="1" 
                   x:Name="cboStatus"
                   Width="200"
                      SelectedIndex="1"
                   Margin="10,5"
                   SelectionChanged="cboStatus_SelectionChanged"
                   HorizontalAlignment="Left"/>

            <!-- Site Selection Panel - will be visible only for Admin -->
            <StackPanel Grid.Column="2" 
                      Orientation="Horizontal"
                      x:Name="siteSelectionPanel"
                      Visibility="Collapsed">
                <TextBlock Text="Fulfill For:" 
                         VerticalAlignment="Center"
                         Margin="10,5,5,5"/>
                <ComboBox x:Name="cboSite"
                         Width="200"
                         Margin="5,5"
                         SelectionChanged="cboSite_SelectionChanged"/>
            </StackPanel>

            <Button Grid.Column="3" 
                 x:Name="btnRefresh" 
                 Content="Refresh" 
                 Style="{StaticResource ButtonInfo}"
                 Click="btnRefresh_Click"
                 Width="80"
                 Margin="5"/>
        </Grid>

        <!-- Orders DataGrid -->
        <GroupBox Grid.Row="1" Header="Available Online Orders" Margin="0,5">
            <DataGrid x:Name="dgvOrders" 
                  Height="150"
                  AutoGenerateColumns="False"
                  SelectionChanged="dgvOrders_SelectionChanged"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  SelectionMode="Single">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding TxnId}" Width="60"/>
                    <DataGridTextColumn Header="Customer" Binding="{Binding CustomerName}" Width="*"/>
                    <DataGridTextColumn Header="Date" Binding="{Binding OrderDate, StringFormat={}{0:MM/dd/yyyy}}" Width="100"/>
                    <DataGridTextColumn Header="Status" Binding="{Binding TxnStatus}" Width="100"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>

        <!-- Main Content Area -->
        <Grid Grid.Row="2" Margin="0,5">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Order Items -->
                <GroupBox Grid.Column="0" Header="Order Items">
                    <DockPanel>
                        <TextBox x:Name="txtSearch"
                              DockPanel.Dock="Top"
                              TextChanged="SearchBox_TextChanged"
                              Style="{StaticResource TextBoxExtend}"
                              hc:InfoElement.Placeholder="Search items by ID or name..."
                              Margin="0,5"/>

                        <DataGrid x:Name="dgvOrderItems" 
                               AutoGenerateColumns="False"
                               SelectionChanged="dgvOrderItems_SelectionChanged"
                               CanUserAddRows="False"
                               IsReadOnly="True"
                               SelectionMode="Single">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="ID" Binding="{Binding ItemId}" Width="60"/>
                                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                                <DataGridTextColumn Header="Required" Binding="{Binding Quantity}" Width="70"/>
                                <DataGridTextColumn Header="In Stock" Binding="{Binding CurrentStock}" Width="70"/>
                            </DataGrid.Columns>
                            <DataGrid.RowStyle>
                                <Style TargetType="DataGridRow">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding InsufficientStock}" Value="True">
                                            <Setter Property="Background" Value="LightPink"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGrid.RowStyle>
                        </DataGrid>
                    </DockPanel>
                </GroupBox>

                <!-- Transfer Buttons -->
                <StackPanel Grid.Column="1" 
                         VerticalAlignment="Center" 
                         Margin="10">
                    <Button x:Name="btnMoveToAssembled" 
                         Content="→" 
                         Style="{StaticResource ButtonPrimary}"
                         Click="btnMoveToAssembled_Click"
                         Width="40"
                         Margin="5"/>
                    <Button x:Name="btnMoveBack" 
                         Content="←" 
                         Style="{StaticResource ButtonDanger}"
                         Click="btnMoveBack_Click"
                         Width="40"
                         Margin="5"/>
                </StackPanel>

                <!-- Prepared Items -->
                <GroupBox Grid.Column="2" Header="Prepared Items">
                    <DataGrid x:Name="dgvPreparedItems" 
                           AutoGenerateColumns="False"
                           SelectionChanged="dgvPreparedItems_SelectionChanged"
                           CanUserAddRows="False"
                           IsReadOnly="True"
                           SelectionMode="Single">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding ItemId}" Width="60"/>
                            <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                            <DataGridTextColumn Header="Quantity" Binding="{Binding Quantity}" Width="80"/>
                        </DataGrid.Columns>
                        <DataGrid.RowStyle>
                            <Style TargetType="DataGridRow">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding PickedUp}" Value="True">
                                        <Setter Property="Background" Value="LightGreen"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.RowStyle>
                    </DataGrid>
                </GroupBox>
            </Grid>

            <!-- Signature Panel -->
            <StackPanel Grid.Row="1" Visibility="Collapsed" x:Name="signaturePanel" Margin="0,10,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Customer Signature Required" 
                            FontWeight="Bold" Margin="5" HorizontalAlignment="Center"/>

                    <Border Grid.Row="1" BorderBrush="Gray" BorderThickness="1" Margin="5">
                        <InkCanvas x:Name="inkSignature" Height="120" Background="#F5F5F5"/>
                    </Border>

                    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="5,10">
                        <Button x:Name="btnConfirmSignature" Content="Confirm Signature" 
                             Click="btnConfirmSignature_Click" 
                             Style="{StaticResource ButtonPrimary}"
                             Width="120" Margin="5,0"/>
                        <Button x:Name="btnClearSignature" Content="Clear Signature" 
                             Click="btnClearSignature_Click" 
                             Style="{StaticResource ButtonDanger}"
                             Width="100" Margin="5,0"/>
                    </StackPanel>
                </Grid>
            </StackPanel>

            <!-- Complete Order Button -->
            <Button Grid.Row="2" x:Name="btnComplete"
                 Content="Complete Order"
                 Style="{StaticResource ButtonSuccess}"
                 Click="btnComplete_Click"
                 Width="150"
                 Margin="5,10,5,5"
                 HorizontalAlignment="Right"/>
        </Grid>
    </Grid>
</UserControl>