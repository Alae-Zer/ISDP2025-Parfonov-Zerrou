<Window x:Class="ISDP2025_Parfonov_Zerrou.Forms.CustomMessageBox"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:local="clr-namespace:ISDP2025_Parfonov_Zerrou.Forms"
      mc:Ignorable="d"
      Title="Select Permission" 
      Height="500" 
      Width="400"
      WindowStartupLocation="CenterScreen"
      ResizeMode="NoResize"
      WindowStyle="ToolWindow" 
      Closing="Window_Closing" Icon="/Forms/bullseye.png"
        xmlns:hc="https://handyorg.github.io/handycontrol">
    <Window.Resources>
        <Style x:Key="RoundedButton" TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                              CornerRadius="5"
                              BorderThickness="1"
                              BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                          VerticalAlignment="Center"
                                          Margin="10,0"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Trigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX"
                                          To="1.1" Duration="0:0:0.1"/>
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY"
                                          To="1.1" Duration="0:0:0.1"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.EnterActions>
                    <Trigger.ExitActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX"
                                          To="1" Duration="0:0:0.1"/>
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY"
                                          To="1" Duration="0:0:0.1"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.ExitActions>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Grid Background="White" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Select Login Permission" 
                 FontSize="20" 
                 FontWeight="Bold"
                 Margin="0,0,0,20"/>
        <StackPanel Grid.Row="1" 
                   Margin="0,0,0,20" 
                   Orientation="Horizontal">
            <Label Content="Default Permission:" 
                  FontWeight="Bold" Height="27" Padding="10,0,10,0"/>
            <TextBlock x:Name="lblDefaultPermission" 
                     Margin="9,6,0,0" 
                     TextWrapping="Wrap"/>
        </StackPanel>
        <GroupBox Grid.Row="2" 
                Header="Additional Permissions" 
                Margin="0,0,0,15" >
            <DataGrid x:Name="dgvPermissions"
                    AutoGenerateColumns="False"
                    IsReadOnly="True"
                    SelectionMode="Single"
                    HeadersVisibility="None" 
                    SelectionChanged="dgvPermissions_SelectionChanged" Height="110.44"
                      RowHeight="25">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Permission" 
                                    Binding="{Binding PermissionName}"
                                    Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>
        <StackPanel Grid.Row="3" 
                  Orientation="Horizontal" 
                  HorizontalAlignment="Right">
            <Button x:Name="btnLogin"
                  Content="Login"
                  MinWidth="150"
                  Height="30"
                  HorizontalAlignment="Left"
                  Margin="0,0,10,0"
                  Style="{StaticResource RoundedButton}"
                  Background="#4CAF50"
                  Foreground="White"
                  Cursor="Hand" 
                  Click="btnLogin_Click"/>
            <Button x:Name="btnCancel"
                  Content="Procceed As Default"
                  MinWidth="100" 
                  HorizontalAlignment="Right"
                  Height="30"
                  Style="{StaticResource RoundedButton}"
                  Background="#FFF62D2D"
                  Foreground="White"
                  Cursor="Hand" 
                  Click="btnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>