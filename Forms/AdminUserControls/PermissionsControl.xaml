<UserControl x:Class="ISDP2025_Parfonov_Zerrou.Forms.AdminUserControls.PermissionsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ISDP2025_Parfonov_Zerrou.Forms.AdminUserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="900"
             Unloaded="UserControl_Unloaded">

    <UserControl.Resources>
        <Style x:Key="RoundedButton" TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="5" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Trigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX"
                                               To="1.1" Duration="0:0:0.1"/>
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY"
                                               To="1.1" Duration="0:0:0.1"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.EnterActions>
                    <Trigger.ExitActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX"
                                               To="1" Duration="0:0:0.1"/>
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY"
                                               To="1" Duration="0:0:0.1"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.ExitActions>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Background="White" Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Permissions Management" 
                   FontSize="24" 
                   FontWeight="Bold" 
                   Margin="0,0,0,20"
                   Grid.Row="0"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <Label Content="Search:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBox x:Name="txtSearch" Width="200" Margin="0,0,10,0" 
                     Cursor="Pen" TextChanged="txtSearch_TextChanged"/>
        </StackPanel>

        <GroupBox Grid.Row="2" Header="Employee Permissions" Padding="10" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Label Content="Employee ID:" Grid.Row="0" Grid.Column="0" FontWeight="Bold"/>
                <TextBlock x:Name="lblEmployeeId" Grid.Row="0" Grid.Column="1" Margin="5" FontWeight="Bold"/>

                <Label Content="Employee Name:" Grid.Row="1" Grid.Column="0"/>
                <TextBlock x:Name="lblEmployeeName" Grid.Row="1" Grid.Column="1" Margin="5"/>

                <Label Content="Position:" Grid.Row="2" Grid.Column="0"/>
                <TextBlock x:Name="lblPosition" Grid.Row="2" Grid.Column="1" Margin="5"/>

                <Label Content="Default Permission:" Grid.Row="3" Grid.Column="0"/>
                <TextBlock x:Name="lblDefaultPermission" Grid.Row="3" Grid.Column="1" Margin="5"/>

                <Label Content="Additional Permissions:" Grid.Row="4" Grid.Column="0"/>
                <DataGrid x:Name="dgvCurrentPermissions" 
                            Grid.Row="4" 
                            Grid.Column="1" 
                            Margin="5"
                            Height="Auto"
                            MaxHeight="300"
                            MaxWidth="250"
                            HorizontalAlignment="Left"
                            AutoGenerateColumns="False"
                            IsReadOnly="True"
                            HeadersVisibility="None" SelectionChanged="dgvCurrentPermissions_SelectionChanged">
                    <DataGrid.Columns>
                        <DataGridTextColumn Binding="{Binding Permission}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>

                <Label Content="Available Permissions:" Grid.Row="5" Grid.Column="0"/>
                <ComboBox x:Name="cmbAvailablePermissions" Grid.Row="5" Grid.Column="1" Margin="5" 
                          IsEnabled="False" Cursor="Hand" SelectionChanged="cmbAvailablePermissions_SelectionChanged"/>
            </Grid>
        </GroupBox>

        <Grid Grid.Row="3" Margin="0,10,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button x:Name="btnRefresh" Content="Refresh" Width="100" Height="30" 
                    Grid.Column="0"
                    Click="BtnRefresh_Click"
                    Style="{StaticResource RoundedButton}"
                    Background="#2196F3"
                    Foreground="White" Cursor="Hand"/>

            <StackPanel Grid.Column="2" Orientation="Horizontal">
                <Button x:Name="btnClear" Content="Clear" Width="100" Height="30" 
                        Margin="0,0,10,0"
                        Style="{StaticResource RoundedButton}"
                        Background="#FFF62D2D"
                        Foreground="White" Cursor="Hand" Click="btnClear_Click"/>
                <Button x:Name="btnEdit" Content="Edit" Width="100" Height="30" 
                        Margin="0,0,10,0" Click="BtnEdit_Click"
                        Style="{StaticResource RoundedButton}"
                        Background="#FFD700"
                        Foreground="Black" Cursor="Hand"/>
                <Button x:Name="btnAddPermission" Content="Add Permission" Width="120" Height="30" 
                        Margin="0,0,10,0"
                        Style="{StaticResource RoundedButton}"
                        Background="#4CAF50"
                        Foreground="White" Cursor="Hand"
                        Visibility="Collapsed" Click="btnAddPermission_Click"/>
                <Button x:Name="btnRemovePermission" Content="Remove Permission" Width="120" Height="30" 
                        Margin="0,0,10,0"
                        Style="{StaticResource RoundedButton}"
                        Background="#FFF62D2D"
                        Foreground="White" Cursor="Hand"
                        Visibility="Collapsed" Click="btnRemovePermission_Click"/>
                <Button x:Name="btnSave" Content="Leave" Width="100" Height="30" 
                        Style="{StaticResource RoundedButton}"
                        Background="#2196F3"
                        Foreground="White" Cursor="Hand"
                        Visibility="Collapsed" Click="btnSave_Click"/>
            </StackPanel>
        </Grid>

        <DataGrid x:Name="dgvEmployees" 
                  Grid.Row="4" 
                  AutoGenerateColumns="False" 
                  IsReadOnly="True"
                  SelectionMode="Single"
                  
                  Margin="0,10,0,0" SelectionChanged="dgvEmployees_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding EmployeeId}" Width="40">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="FontWeight" Value="Bold"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="First Name" Binding="{Binding FirstName}" Width="120"/>
                <DataGridTextColumn Header="Last Name" Binding="{Binding LastName}" Width="*"/>
                <DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="*"/>
                <DataGridTextColumn Header="Position" Binding="{Binding PositionName}" Width="*"/>
                <DataGridTextColumn Header="Default Permission" Binding="{Binding DefaultPermission}" Width="*"/>
                <DataGridTextColumn Header="Active" Binding="{Binding IsActive}" Width="60"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>