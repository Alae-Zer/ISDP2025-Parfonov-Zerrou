<UserControl x:Class="ISDP2025_Parfonov_Zerrou.Forms.FloorGuyUserControl.FloorGuyFulfil"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header Section -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Order Assembly Management" 
                      Style="{StaticResource TextBlockLarge}" 
                      Margin="5"/>

            <Button Grid.Column="1" 
                    x:Name="btnRefresh" 
                    Content="Refresh" 
                    Style="{StaticResource ButtonInfo}"
                    Click="btnRefresh_Click"
                    Width="80"
                    Margin="5"/>
        </Grid>

        <!-- Orders DataGrid -->
        <GroupBox Grid.Row="1" Header="Available Orders" Margin="0,5">
            <DataGrid x:Name="dgvOrders" 
                     Height="150"
                     AutoGenerateColumns="False"
                     SelectionChanged="dgvOrders_SelectionChanged"
                     CanUserAddRows="False"
                     IsReadOnly="True"
                     SelectionMode="Single">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding TxnId}" Width="60"/>
                    <DataGridTextColumn Header="Store" Binding="{Binding StoreName}" Width="150"/>
                    <DataGridTextColumn Header="Created" Binding="{Binding CreatedDate, StringFormat={}{0:MM/dd/yyyy}}" Width="100"/>
                    <DataGridTextColumn Header="Ship Date" Binding="{Binding ShipDate, StringFormat={}{0:MM/dd/yyyy}}" Width="100"/>
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100"/>
                    <DataGridTextColumn Header="Assembler" Binding="{Binding AssemblerName}" Width="120"/>
                    <DataGridTextColumn Header="Notes" Binding="{Binding Notes}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>

        <!-- Main Content Area -->
        <Grid Grid.Row="2" Margin="0,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Order Items -->
            <GroupBox Grid.Column="0" Header="Order Items">
                <DockPanel>
                    <TextBox x:Name="txtSearch"
                             DockPanel.Dock="Top"
                             TextChanged="SearchBox_TextChanged"
                             Style="{StaticResource TextBoxExtend}"
                             hc:InfoElement.Placeholder="Search items by ID or name..."
                             Margin="0,5"/>

                    <DataGrid x:Name="dgvOrderItems" 
                              AutoGenerateColumns="False"
                              SelectionChanged="dgvOrderItems_SelectionChanged"
                              CanUserAddRows="False"
                              IsReadOnly="True"
                              SelectionMode="Single">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding ItemId}" Width="60"/>
                            <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                            <DataGridTextColumn Header="Required" Binding="{Binding Required}" Width="80"/>
                            <DataGridTextColumn Header="Remaining" Binding="{Binding Remaining}" Width="80"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </DockPanel>
            </GroupBox>

            <!-- Transfer Buttons -->
            <StackPanel Grid.Column="1" 
                        VerticalAlignment="Center" 
                        Margin="10">
                <Button x:Name="btnMoveToAssembled" 
                        Content="→" 
                        Style="{StaticResource ButtonPrimary}"
                        Click="btnMoveToAssembled_Click"
                        Width="40"
                        Margin="5"/>
                <Button x:Name="btnMoveBack" 
                        Content="←" 
                        Style="{StaticResource ButtonDanger}"
                        Click="btnMoveBack_Click"
                        Width="40"
                        Margin="5"/>
            </StackPanel>

            <!-- Assembled Items -->
            <GroupBox Grid.Column="2" Header="Assembled Items">
                <DataGrid x:Name="dgvAssembledItems" 
                          AutoGenerateColumns="False"
                          SelectionChanged="dgvAssembledItems_SelectionChanged"
                          CanUserAddRows="False"
                          IsReadOnly="True"
                          SelectionMode="Single">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding ItemId}" Width="60"/>
                        <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                        <DataGridTextColumn Header="Required" Binding="{Binding Required}" Width="80"/>
                        <DataGridTextColumn Header="Assembled" Binding="{Binding Assembled}" Width="80"/>
                    </DataGrid.Columns>
                </DataGrid>
            </GroupBox>
        </Grid>

        <!-- Barcode Scanner Section -->
        <GroupBox Grid.Row="3" Header="Barcode Scanner" Margin="0,5,0,0">
            <Grid Margin="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox x:Name="txtBarcode"
                         KeyDown="Barcode_KeyDown"
                         Style="{StaticResource TextBoxExtend}"
                         hc:InfoElement.Placeholder="Scan barcode..."
                         Margin="5,0"/>

                <TextBlock Grid.Column="1"
                         Text="Scan barcode or press Enter"
                         VerticalAlignment="Center"
                         Margin="10,0"/>   

                <Button Grid.Column="2"
                        x:Name="btnComplete"
                        Content="Complete Assembly"
                        Style="{StaticResource ButtonSuccess}"
                        Click="btnComplete_Click"
                        Width="150"
                        Margin="5,0"
                        IsEnabled="False"/>
            </Grid>
        </GroupBox>
    </Grid>
</UserControl>