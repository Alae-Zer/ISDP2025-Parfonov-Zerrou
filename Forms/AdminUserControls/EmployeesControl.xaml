<UserControl x:Class="ISDP2025_Parfonov_Zerrou.Forms.AdminUserControls.EmployeesControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ISDP2025_Parfonov_Zerrou.Forms.AdminUserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <Grid Background="White" Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Title -->
            <RowDefinition Height="Auto"/>
            <!-- Form -->
            <RowDefinition Height="Auto"/>
            <!-- Button -->
            <RowDefinition Height="*"/>
            <!-- DataGrid -->
        </Grid.RowDefinitions>

        <!-- Title Section -->
        <TextBlock Text="Employee Management" 
               FontSize="24" 
               FontWeight="Bold" 
               Margin="0,0,0,20"/>

        <!-- Employee Form -->
        <GroupBox Grid.Row="1" Header="Employee Information" Padding="10" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Left Column Labels -->
                <Label Content="First Name:" Grid.Row="0" Grid.Column="0"/>
                <Label Content="Last Name:" Grid.Row="1" Grid.Column="0"/>
                <Label Content="Employee ID:" Grid.Row="2" Grid.Column="0"/>
                <Label Content="Position:" Grid.Row="3" Grid.Column="0"/>
                <Label Content="Email:" Grid.Row="4" Grid.Column="0"/>

                <!-- Left Column Controls -->
                <TextBox x:Name="txtFirstName" Grid.Row="0" Grid.Column="1" Margin="5"/>
                <TextBox x:Name="txtLastName" Grid.Row="1" Grid.Column="1" Margin="5"/>
                <TextBox x:Name="txtEmployeeID" Grid.Row="2" Grid.Column="1" Margin="5"/>
                <ComboBox x:Name="cmbPosition" Grid.Row="3" Grid.Column="1" Margin="5"/>
                <TextBox x:Name="txtEmail" Grid.Row="4" Grid.Column="1" Margin="5"/>

                <!-- Right Column Labels -->
                <Label Content="Username:" Grid.Row="0" Grid.Column="2"/>
                <Label Content="Password:" Grid.Row="1" Grid.Column="2"/>
                <Label Content="Location:" Grid.Row="2" Grid.Column="2"/>
                <Label Content="Active Status:" Grid.Row="3" Grid.Column="2"/>
                
                <!-- Right Column Controls -->
                <TextBox x:Name="txtUsername" Grid.Row="0" Grid.Column="3" Margin="5"/>
                <PasswordBox x:Name="txtPassword" Grid.Row="1" Grid.Column="3" Margin="5"/>
                <ComboBox x:Name="cmbLocation" Grid.Row="2" Grid.Column="3" Margin="5"/>
                <CheckBox x:Name="chkActive" Grid.Row="3" Grid.Column="3" Margin="5" VerticalAlignment="Center" Content="Active"/>
                
            </Grid>
        </GroupBox>

        <!-- Refresh Button -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="2,10,29,10" HorizontalAlignment="Right">
            <Button x:Name="btnRefresh" Content="Refresh" Width="100" Height="30" Click="BtnRefresh_Click"/>
            <Button x:Name="Clear" Content="Clear"  Width="100" Height="30" Grid.Row="4" Grid.Column="2"/>
            <Button x:Name="Add" Content="Add" Width="100" Height="30" />
        </StackPanel>

        <!-- Employee List -->
        <DataGrid x:Name="dgEmployees" 
              Grid.Row="3" 
              AutoGenerateColumns="False" 
              IsReadOnly="True"
              SelectionMode="Single"
              SelectionChanged="DgEmployees_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding EmployeeId}" Width="80"/>
                <DataGridTextColumn Header="First Name" Binding="{Binding FirstName}" Width="120"/>
                <DataGridTextColumn Header="Last Name" Binding="{Binding LastName}" Width="120"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="200"/>
                <DataGridTextColumn Header="Position" Binding="{Binding Posn.PermissionLevel}" Width="120"/>
                <DataGridTextColumn Header="Location" Binding="{Binding Site.SiteName}" Width="120"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Active}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
