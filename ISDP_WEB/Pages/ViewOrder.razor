@page "/view-order"
@page "/view-order/{OrderId:int}"
@using ISDP2025_Parfonov_Zerrou.Models
@using Microsoft.EntityFrameworkCore
@inject BestContext Context
@inject ISnackbar Snackbar

<MudContainer MaxWidth="MaxWidth.Large" Class="pt-8 pb-8">
    <MudText Typo="Typo.h4" Class="mb-4">Track Your Order</MudText>

    <MudPaper Class="pa-4 mb-4">
        <MudText Typo="Typo.body1" Class="mb-3">
            Enter your order details to check the status of your order.
        </MudText>

        <MudGrid>
            <MudItem xs="12" md="6">
                <MudTextField @bind-Value="searchOrderId" Label="Order ID" 
                Variant="Variant.Outlined" Class="mb-3" />
            </MudItem>
            <MudItem xs="12" md="6">
                <MudTextField @bind-Value="searchEmail" Label="Email Address" 
                Variant="Variant.Outlined" Class="mb-3" />
            </MudItem>
            <MudItem xs="12">
                <MudButton Variant="Variant.Filled" Color="Color.Primary" 
                OnClick="SearchOrder" Class="mr-2"
                StartIcon="@Icons.Material.Filled.Search">
                    Search
                </MudButton>
                <MudButton Variant="Variant.Outlined" Color="Color.Secondary" 
                OnClick="ClearSearch">
                    Clear
                </MudButton>
            </MudItem>
        </MudGrid>
    </MudPaper>

    @if (isLoading)
    {
        <div class="d-flex justify-center my-4">
            <MudProgressCircular Color="Color.Primary" Indeterminate="true" />
        </div>
    }
    else if (order != null)
    {
        <MudPaper Class="pa-4 mb-4">
            <MudGrid>
                <MudItem xs="12">
                    <MudText Typo="Typo.h6" Class="mb-3">Order Details</MudText>
                </MudItem>

                <MudItem xs="12" md="6">
                    <MudList T="string">
                        <MudListItem>
                            <MudText Typo="Typo.body1">
                                <strong>Order ID:</strong> @order.TxnId
                            </MudText>
                        </MudListItem>
                        <MudListItem>
                            <MudText Typo="Typo.body1">
                                <strong>Delivery Date:</strong> @order.ShipDate.ToString("MMM dd, yyyy hh:mm tt")
                            </MudText>
                        </MudListItem>
                        <MudListItem>
                            <MudText Typo="Typo.body1">
                                <strong>Order Made by:</strong> <br> @foreach (var status in @order.Notes.Split(","))
                                {
                                    <MudText class="pt-1">@status</MudText>
                                }
                            </MudText>
                        </MudListItem>
                    </MudList>
                </MudItem>

                <MudItem xs="12" md="6">
                    <MudList T="string">
                        <MudListItem>
                            <MudText Typo="Typo.body1">
                                <strong>Store:</strong> @(order.SiteIdtoNavigation?.SiteName)
                            </MudText>
                        </MudListItem>
                        <MudListItem>
                            <MudText Typo="Typo.body1">
                                <strong>Status:</strong>
                                <MudChip Color="@GetStatusColor(order.TxnStatus)" Size="Size.Small">@order.TxnStatus</MudChip>
                            </MudText>
                        </MudListItem>
                    </MudList>
                </MudItem>


                <MudItem xs="12">
                    <MudExpansionPanels>
                        <MudExpansionPanel Text="Order Items" IsInitiallyExpanded="true">
                            <MudTable Items="@orderItems" Hover="true" Breakpoint="Breakpoint.Sm" LoadingProgressColor="Color.Info">
                                <HeaderContent>
                                    <MudTh>Item</MudTh>
                                    <MudTh>Quantity</MudTh>
                                </HeaderContent>
                                <RowTemplate>
                                    <MudTd DataLabel="Item">@context.Item.Name</MudTd>
                                    <MudTd DataLabel="Quantity">@context.Quantity</MudTd>
                                </RowTemplate>
                            </MudTable>
                        </MudExpansionPanel>
                    </MudExpansionPanels>
                </MudItem>

                <MudItem xs="12" Class="mt-4">
                    <MudText Typo="Typo.h6">Order Progress</MudText>
                    <MudTimeline TimelineOrientation="TimelineOrientation.Horizontal" Class="mb-8">
                        @foreach (var status in GetOrderStatuses(order.TxnStatus))
                        {
                            <MudTimelineItem Color="@(status.IsCompleted ? Color.Success : Color.Default)"
                            Elevation="1">
                                <ItemContent>
                                    <MudText Typo="Typo.h6">@status.Name</MudText>
                                    <MudText Typo="Typo.body2">@status.Description</MudText>
                                </ItemContent>
                            </MudTimelineItem>
                        }
                    </MudTimeline>
                </MudItem>

                <MudItem xs="12">
                    <MudPaper Class="pa-3 mt-3" Elevation="0">
                        <MudText Typo="Typo.h6" Align="Align.Right">
                            Total Items: @orderItems.Sum(i => i.Quantity)
                        </MudText>
                    </MudPaper>
                </MudItem>
            </MudGrid>
        </MudPaper>
    }else if (orders != null && orders.Any())
    {
        <!-- Display list of orders -->
        <MudPaper Class="pa-4 mb-4">
            <MudText Typo="Typo.h6" Class="mb-3">Your Orders</MudText>
            <MudTable Items="@orders" Hover="true" Breakpoint="Breakpoint.Sm">
                <HeaderContent>
                    <MudTh>Order ID</MudTh>
                    <MudTh>Date</MudTh>
                    <MudTh>Status</MudTh>
                    <MudTh>Action</MudTh>
                </HeaderContent>
                <RowTemplate>
                    <MudTd>@context.TxnId</MudTd>
                    <MudTd>@context.CreatedDate.ToString("MMM dd, yyyy")</MudTd>
                    <MudTd>
                        <MudChip T="string" Color="@GetStatusColor(context.TxnStatus)" Size="Size.Small">@context.TxnStatus</MudChip>
                    </MudTd>
                    <MudTd>
                        <MudButton Variant="Variant.Text" Color="Color.Primary" 
                        OnClick="@(() => LoadOrderDetails(context.TxnId))">
                            View
                        </MudButton>
                    </MudTd>
                </RowTemplate>
            </MudTable>
        </MudPaper>
    }
    else if (showNoOrderFound)
    {
        <MudAlert Severity="Severity.Warning" Class="my-4">
            No order found with the provided information. Please check your order ID and email address.
        </MudAlert>
    }
</MudContainer>

@code {
    [Parameter]
    public int? OrderId { get; set; }

    private string searchOrderId = "";
    private string searchEmail = "";
    private bool isLoading = false;
    private bool showNoOrderFound = false;

    private Txn order;
    private List<Txn> orders = new List<Txn>();
    private List<Txnitem> orderItems = new List<Txnitem>();
    private List<Txnaudit> orderAudit = new List<Txnaudit>();

    private List<List<Txnitem>> ordersItems = new List<List<Txnitem>>();
    private List<List<Txnaudit>> ordersAudit = new List<List<Txnaudit>>();

    private List<OrderStatus> GetOrderStatuses(string currentStatus)
    {
        var statuses = new List<OrderStatus>
    {
        new OrderStatus { Name = "Order Placed", Description = "Your order has been received by our system.", IsCompleted = true },
        new OrderStatus { Name = "Processing", Description = "Store staff are reviewing your order.", IsCompleted = currentStatus != "NEW" },
        new OrderStatus { Name = "Assembling", Description = "Your items are being gathered and prepared.", IsCompleted = currentStatus == "ASSEMBLING" || currentStatus == "ASSEMBLED" || currentStatus == "READY" || currentStatus == "COMPLETE" },
        new OrderStatus { Name = "Ready for Pickup", Description = "Your order is ready to be picked up from the store.", IsCompleted = currentStatus == "READY" || currentStatus == "COMPLETE" },
        new OrderStatus { Name = "Completed", Description = "Your order has been picked up and completed.", IsCompleted = currentStatus == "COMPLETE" }
    };

        return statuses;
    }

    public class OrderStatus
    {
        public string Name { get; set; }
        public string Description { get; set; }
        public bool IsCompleted { get; set; }
    }

    protected override async Task OnInitializedAsync()
    {
        if (OrderId.HasValue)
        {
            searchOrderId = OrderId.Value.ToString();
            await SearchOrder();
        }
    }

    private async Task SearchOrder()
    {
        showNoOrderFound = false;
        isLoading = true;
        order = null; // Clear single order
        orders = null; // Clear orders list
        StateHasChanged();

        try
        {
            if (int.TryParse(searchOrderId, out int orderId))
            {
                // Search by Order ID
                order = await Context.Txns
                    .Include(t => t.SiteIdtoNavigation)
                    .FirstOrDefaultAsync(t => t.TxnId == orderId && t.TxnType == "Online");
                
                if (order != null)
                    await LoadOrderDetails(order.TxnId);
            }
            else if (!string.IsNullOrWhiteSpace(searchEmail))
            {
                // Search by Email
                orders = await Context.Txns
                    .Include(t => t.SiteIdtoNavigation)
                    .Where(t => t.Notes.Contains(searchEmail) && t.TxnType == "Online")
                    .OrderByDescending(t => t.CreatedDate)
                    .ToListAsync();

                if (!orders.Any())
                    showNoOrderFound = true;
            }
            else
            {
                Snackbar.Add("Please enter an Order ID or Email address", Severity.Warning);
            }
        }
        catch (Exception ex)
        {
            Snackbar.Add($"Error retrieving order: {ex.Message}", Severity.Error);
        }
        finally
        {
            isLoading = false;
            StateHasChanged();
        }
    }

    private async Task LoadOrderDetails(int orderId)
    {
        isLoading = true;
        StateHasChanged();

        try
        {
            order = await Context.Txns
                .Include(t => t.SiteIdtoNavigation)
                .FirstOrDefaultAsync(t => t.TxnId == orderId);

            if (order != null)
            {
                orderItems = await Context.Txnitems
                    .Include(ti => ti.Item)
                    .Where(ti => ti.TxnId == orderId)
                    .ToListAsync();

                orderAudit = await Context.Txnaudits
                    .Where(ta => ta.TxnId == orderId)
                    .OrderByDescending(ta => ta.CreatedDate)
                    .ToListAsync();

                orders = null; // Clear the list to show details
            }
        }
        catch (Exception ex)
        {
            Snackbar.Add($"Error loading order details: {ex.Message}", Severity.Error);
        }
        finally
        {
            isLoading = false;
            StateHasChanged();
        }
    }

    private void ClearSearch()
    {
        searchOrderId = "";
        searchEmail = "";
        order = null;
        orderItems.Clear();
        orderAudit.Clear();
        showNoOrderFound = false;
    }

    private Color GetStatusColor(string status)
    {
        return status switch
        {
            "NEW" => Color.Info,
            "SUBMITTED" => Color.Info,
            "RECEIVED" => Color.Info,
            "ASSEMBLING" => Color.Warning,
            "ASSEMBLED" => Color.Success,
            "READY" => Color.Success,
            "COMPLETE" => Color.Success,
            "CANCELLED" => Color.Error,
            "REJECTED" => Color.Error,
            _ => Color.Default
        };
    }

    private string GetStatusMessage(string status)
    {
        return status switch
        {
            "NEW" => "Your order has been received and is waiting to be processed.",
            "SUBMITTED" => "Your order has been submitted and is waiting to be processed.",
            "RECEIVED" => "Your order has been received by the store and will be assembled soon.",
            "ASSEMBLING" => "Your order is currently being assembled by our staff.",
            "ASSEMBLED" => "Your order has been assembled and will be ready for pickup soon.",
            "READY" => "Your order is ready for pickup! Please come to the store with your order ID.",
            "COMPLETE" => "Your order has been completed. Thank you for shopping with us!",
            "CANCELLED" => "Your order has been cancelled. Please contact customer service for assistance.",
            "REJECTED" => "Your order has been rejected. Please contact customer service for assistance.",
            _ => "Your order status is being updated. Please check back later."
        };
    }
}